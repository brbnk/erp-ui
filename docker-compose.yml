version: "3.7"

volumes:
  node_modules:

services:
  ui:
    restart: "no"
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/app
      - node_modules:/app/node_modules
    ports:
      - 3000:3000